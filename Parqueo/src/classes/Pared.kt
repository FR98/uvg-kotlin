package main.classes

class Pared (
        private var posX: Int,
        private var posY: Int
) {
    fun getPosX(): Int {
        return posX
    }

    fun getPosY(): Int {
        return posY
    }
}